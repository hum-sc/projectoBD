package view;

import javax.swing.*;
import java.awt.*;

public class Panel extends JPanel {
    public Panel(int width, int height, Color color){
        super();
        setSize(width, height);
        setBackground(color);
    }
}
